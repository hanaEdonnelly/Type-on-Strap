# SITE CONFIGURATION
baseurl: "/Type-on-Strap"
url: "https://carolinawomenshockey.com"

# THEME-SPECIFIC CONFIGURATION
title: USC Women's Hockey                                  # site's title
description: "The official website of Lady Gamecocks Hockey"      # used by search engines
avatar:                         # Empty for no avatar in navbar
favicon: assets/favicon.png                             # Icon displayed in the tab

# Header and footer text
header_text: The Home of Gamecock Hockey  # Change Blog header text
header_feature_image: assets/img/header/jerseys-back.jpg
header_feature_image_responsive: true
footer_text: >
  Created by <a href='https:hana.gallery'>Hana Donnelly</a> with <a href='https://github.com/sylhare/Type-on-Strap'>Type on Strap</a>

# Plugins 
plugins: 
  - jekyll-paginate
  - jekyll-seo-tag
  - jekyll-feed


# Blog
excerpt: true                                           # Or "truncate" (first 250 characters), "false" to disable
post_navigation: true
color_image: /assets/img/lineart.png            # A bit transparent for color posts.

# Features
# More in the _data folder for share buttons, author and language
# For layout customization, go to the "_sass > base" folder, and check "_variables.scss"
katex: true                                             # Enable if using math markup
mermaid: default                                        # Enable mermaid-js for diagrams, use theme: base, forest, dark, default, neutral
google_analytics:                                       # Measurement ID, e.g. "G-00000"
cookie_consent: true                                   # To respect the usage of cookies
color_theme: auto                                       # auto, dark or light

# Comments options
comments:
  disqus_shortname:                                     # Your discus shortname for comments
  cusdis_app_id:                                        # Your cusdis data-app-id
  utterances:                                           # Enable by filling below information. For more info, go to https://utteranc.es
    repo:                                               # your public comments repository (e.g. owner/repo)
    issue-term:                                         # Issue term (e.g. "comment" consider issues with this word in the title as comments)

# PAGINATION
pagination:
  enabled: true
  per_page: 5
  permalink: '/news/page:num/'


# PORTFOLIO
collections:
  portfolio:
    output: true
    permalink: /:collection/:name
  games: 
    output: true
    permalink: /games/:name
  roster:
    output: true 
    permalink: /roster/:title/


# BUILD SETTINGS
sass:
  style: compressed
exclude: [".jekyll-cache", ".jekyll-metadata", ".idea", "vendor/*", "assets/node_modules/*"]

# theme: type-on-strap                                  # if using the theme as a jekyll theme gem
remote_theme: sylhare/Type-on-Strap                     # If using as a remote_theme in github
